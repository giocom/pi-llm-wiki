{
  "name": "pi-llm-wiki",
  "version": "0.8.0",
  "description": "Pi extension that brings llm-wiki workflows to Pi, one feature at a time. v0.8: multi-compile + dedup + tag filter + auto context. v0.7: turndown. v0.6: /wiki:add. v0.5: /wiki:search. v0.4: lint+wiki index. v0.3: auto raw index. v0.2: list/show/compile/query. v0.1: ingest.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi-extension",
    "llm-wiki",
    "wiki",
    "ingest",
    "rag"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run",
    "check": "npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.81.1",
    "@earendil-works/pi-coding-agent": "^0.80.2",
    "@types/node": "^26.0.0",
    "@types/turndown": "^5.0.5",
    "typebox": "^1.1.38",
    "typescript": "^5.8.0",
    "vitest": "^4.1.7"
  }
}
